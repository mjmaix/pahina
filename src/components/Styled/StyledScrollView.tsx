import styled from 'styled-components/native';

import { TabBarAwareScrollView } from '../Navigation/TabBarAwareScrollView';

export const StyledScrollView = styled(TabBarAwareScrollView).attrs(
  props => ({}),
)``;
